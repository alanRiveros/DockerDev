version: '2'
services:
  nginx:
    build: nginx/
    ports:
     - "5000:80"
    volumes:
     - /code/phpinfo:/var/www/html
     - /etc/nginx/sites-available:/etc/nginx/sites-enabled
    depends_on:
     - redis
  redis:
    image: redis
